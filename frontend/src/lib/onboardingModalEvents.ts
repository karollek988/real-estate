/** Cross-component signal to open the "how it works" onboarding modal (triggered from SiteHeader nav and the hero CTA). */
export const OPEN_ONBOARDING_MODAL_EVENT = "open-onboarding-modal";

/** Fired when the modal's CTA closes and the user should land back in the URL input. */
export const FOCUS_URL_INPUT_EVENT = "focus-url-input";
